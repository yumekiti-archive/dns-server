version: '3'

services:

    dns:
        build: ./
        tty: true
        volumes:
            - ./bind/:/mnt/bind
            - ./ip.txt/:/mnt/ip.txt
        networks:
            - default
            
#    web:
#        image: httpd:alpine
#        ports:
#            - "8080:80"
#        command: apk add net-tools && ifconfig >> /mnt/ip.txt
networks:
    default:
        driver: bridge